server:
  port: ${PORT:8082}
spring:
  data:
    mongodb:
      uri: mongodb+srv://<username>:<password>@cluster0.2yyj6si.mongodb.net/rating_service
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
eureka:
  instance:
    prefer-ip-address: true
  client:
    fetch-registry: true
    register-with-eureka: true
    service-url:
      defaultZone: http://localhost:8761/eureka/